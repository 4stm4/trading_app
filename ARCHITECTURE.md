# 🏗️ Архитектура торговой системы

## 📊 Общая структура

```
trading_app/
├── adapters/              # Адаптеры для различных бирж
│   ├── moex/             # Московская биржа
│   ├── binance/          # Binance (в разработке)
│   └── alfa_invest/      # Альфа Инвестиции (в разработке)
│
├── services/             # Бизнес-логика
│   └── strategy_engine/  # 🎯 Универсальный движок стратегий
│
├── entities/             # Модели данных
├── frontend/             # Веб-интерфейс
└── settings.py          # Конфигурация
```

## 🎯 Strategy Engine (Ключевой компонент)

### Расположение
```
services/strategy_engine/
├── __init__.py       # Публичный API
├── core.py          # ATR, структура, объемы
├── signals.py       # Генерация сигналов
├── models.py        # 5 торговых моделей
├── risk.py          # Риск-менеджмент
└── backtest.py      # Бэктестинг
```

### Принципы

1. **Универсальность** - работает с любым адаптером
2. **Модульность** - легко расширяется
3. **Независимость** - не зависит от источника данных
4. **Централизация** - вся торговая логика в одном месте

### Как это работает

```python
# Адаптер загружает данные
from adapters.moex import load_data_with_indicators
df = load_data_with_indicators('SBER', '1h')

# Strategy Engine обрабатывает
from services.strategy_engine import get_model, generate_signal
model = get_model('conservative')
signal = generate_signal(df, deposit=100000, model=model)

# Результат: универсальный сигнал
signal.signal     # 'long', 'short', 'none'
signal.entry      # Точка входа
signal.stop       # Стоп-лосс
signal.rr         # Risk/Reward
```

## 📦 Адаптеры

### Принцип работы

Каждый адаптер:
1. Подключается к своему источнику данных (API биржи)
2. Загружает OHLCV данные
3. Добавляет индикаторы (MA, RSI)
4. Передает в Strategy Engine
5. Получает универсальный сигнал

### Пример: MOEX

```
adapters/moex/
├── moex.py          # API клиент MOEX
├── indicators.py    # Расчет MA, RSI
├── trading_cli.py   # CLI интерфейс
└── README.md        # Документация
```

**Используется:**
- `services/strategy_engine` для сигналов
- `services/strategy_engine` для бэктестинга
- `services/strategy_engine` для моделей

### Пример: Binance (будущее)

```
adapters/binance/
├── binance.py       # API клиент Binance
├── indicators.py    # Расчет индикаторов
└── trading_cli.py   # CLI интерфейс
```

**Будет использовать:**
- `services/strategy_engine` (тот же самый!)
- Нет дублирования кода
- Единая торговая логика

## 🔄 Поток данных

```
┌─────────────────┐
│  Биржа (MOEX)   │
│   API запрос    │
└────────┬────────┘
         │
         ↓
┌─────────────────┐
│  Adapter MOEX   │
│  - get_candles  │
│  - add MA/RSI   │
└────────┬────────┘
         │
         ↓ DataFrame (OHLCV + индикаторы)
         │
┌─────────────────────────┐
│   Strategy Engine       │
│   - analyze_structure   │
│   - check_conditions    │
│   - calculate_risk      │
│   - generate_signal     │
└────────┬────────────────┘
         │
         ↓ TradingSignal
         │
┌─────────────────┐
│  User / CLI     │
│  Получает       │
│  сигнал         │
└─────────────────┘
```

## 🎨 Дизайн-паттерны

### 1. Strategy Pattern
Разные торговые модели реализуют одинаковый интерфейс:
```python
class TradingModel:
    min_rr: float
    trend_required: bool
    # ... другие параметры
```

### 2. Adapter Pattern
Разные биржи адаптируют данные к единому формату:
```python
# MOEX Adapter
df = moex_adapter.get_candles()  # → DataFrame

# Binance Adapter (будущее)
df = binance_adapter.get_candles()  # → тот же DataFrame

# Strategy Engine работает с обоими!
signal = generate_signal(df, model)
```

### 3. Dependency Injection
Адаптеры передаются в функции:
```python
def load_data(ticker, adapter=None):
    if adapter is None:
        adapter = MOEXAdapter()
    return adapter.get_candles(ticker)
```

## 🔧 Расширение системы

### Добавление новой биржи

1. Создать адаптер:
```python
# adapters/new_exchange/client.py
class NewExchangeAdapter:
    def get_candles(self, symbol, timeframe):
        # Загрузка данных
        return df  # OHLCV DataFrame
```

2. Добавить индикаторы:
```python
# adapters/new_exchange/indicators.py
from services.strategy_engine import calculate_atr

def add_indicators(df):
    df['ma50'] = df['close'].rolling(50).mean()
    df['ma200'] = df['close'].rolling(200).mean()
    df['rsi'] = calculate_rsi(df)
    df['atr'] = calculate_atr(df)
    return df
```

3. Использовать Strategy Engine:
```python
# adapters/new_exchange/trading.py
from services.strategy_engine import generate_signal, get_model

df = load_and_prepare_data()
model = get_model('balanced')
signal = generate_signal(df, deposit=100000, model=model)
```

### Добавление новой модели

```python
# В файле или новом модуле
from services.strategy_engine import TradingModel

CUSTOM_MODELS = {
    "my_model": TradingModel(
        name="my_model",
        description="Моя стратегия",
        min_rr=2.5,
        max_risk_percent=1.0,
        # ... параметры
    )
}

# Использование
from services.strategy_engine import MODELS
MODELS.update(CUSTOM_MODELS)
```

### Добавление нового индикатора

```python
# services/strategy_engine/core.py
def calculate_new_indicator(df: pd.DataFrame) -> pd.Series:
    """Новый индикатор"""
    return df['close'].rolling(20).std()
```

## 📊 Требования к данным

Любой адаптер должен предоставить DataFrame с колонками:

| Колонка | Тип | Описание |
|---------|-----|----------|
| `open` | float | Цена открытия |
| `high` | float | Максимум |
| `low` | float | Минимум |
| `close` | float | Цена закрытия |
| `volume` | float | Объем |
| `ma50` | float | MA 50 |
| `ma200` | float | MA 200 |
| `rsi` | float | RSI |

Index: `pd.DatetimeIndex`

## 🎯 Преимущества архитектуры

### 1. Централизация логики
- Вся торговая логика в `strategy_engine`
- Не дублируется между адаптерами
- Легко тестировать и улучшать

### 2. Переиспользование кода
- Один движок для всех бирж
- Общие модели риска
- Единый бэктестинг

### 3. Расширяемость
- Новые биржи легко добавить
- Новые модели просто создать
- Новые индикаторы централизованы

### 4. Тестируемость
- Strategy Engine тестируется независимо
- Адаптеры тестируются отдельно
- Интеграционные тесты простые

### 5. Поддерживаемость
- Изменения в одном месте
- Понятная структура
- Хорошая документация

## 🔍 Сравнение: До и После

### До (старая архитектура)
```
adapters/moex/
├── trading_system.py    # 500 строк логики
├── models.py            # Модели
├── risk.py              # Риск
├── backtest.py          # Бэктест
└── strategy.py          # Стратегия

adapters/binance/
├── trading_system.py    # 500 строк ТОЙ ЖЕ логики (дубль!)
├── models.py            # Дубль!
├── risk.py              # Дубль!
└── ...
```
❌ Дублирование кода
❌ Сложно поддерживать
❌ Изменения в N местах

### После (новая архитектура)
```
services/strategy_engine/  # ← Вся логика здесь!
├── core.py
├── signals.py
├── models.py
├── risk.py
└── backtest.py

adapters/moex/            # Только API и данные
├── moex.py
└── indicators.py

adapters/binance/         # Только API и данные
├── binance.py
└── indicators.py
```
✅ Нет дублирования
✅ Легко поддерживать
✅ Изменения в 1 месте

## 📚 Документация

- **Strategy Engine**: `services/strategy_engine/README.md`
- **MOEX Adapter**: `adapters/moex/README.md`
- **Эта архитектура**: `ARCHITECTURE.md`

## 🚀 Быстрый старт

```bash
# 1. Использование готовой системы
python -m adapters.moex.trading_cli SBER -d 100000 --model conservative

# 2. Программное использование
python
>>> from services.strategy_engine import get_model, generate_signal
>>> from adapters.moex import load_data_with_indicators
>>> df = load_data_with_indicators('SBER', '1h')
>>> signal = generate_signal(df, 100000, get_model('balanced'))
>>> print(signal.signal)
```

## 🎓 Принципы

1. **DRY** (Don't Repeat Yourself) - не дублируем код
2. **SOLID** - чистая архитектура
3. **Separation of Concerns** - разделение ответственности
4. **Plugin Architecture** - адаптеры как плагины

---

**Версия:** 2.0.0
**Дата:** 2026-02-14
**Статус:** Production Ready ✅
